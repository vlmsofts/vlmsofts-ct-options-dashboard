__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/
*.egg-info/
dist/
build/
.DS_Store

# Local data files — machine-specific, never commit
local_*.csv
*.BACKUP.csv
*.bak
*.bak_repair

# Runtime state — machine-specific, never commit
settle_status.json
settle_status_kc.json
bloomberg_backup/
ERRORS.md

# ICE Connect API feed — runtime artifact written by the 32-bit producer
api_feed/

# Pre-clean CSV backups
*.precln

# Claude Code internal
.claude/
restart_server.py
